#pragma once

#include "../Opcode.h"

namespace emu
{
	namespace cpu
	{
		namespace z80
		{
			namespace opcodes
			{
				//Bitwise OR B with A
				static u16 OR_B(const Opcode& opcode, const OpcodeParams& params, Registers& regs, Bus& bus)
				{
					return 0;
				}
			}
		}
	}
}